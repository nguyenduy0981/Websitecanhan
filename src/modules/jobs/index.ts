// Public API of the `jobs` module.
// Other modules must import only from this file — never reach into
// `src/modules/jobs/*` internals directly.
//
// Milestone 0 stub. Background jobs run as idempotent cron routes protected
// by CRON_SECRET (see CLAUDE.md). Implemented incrementally from
// Milestone 7 onward.
export {};
